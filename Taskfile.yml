version: '3'

includes:
  bench: .tasks/bench.yml
  docs: .tasks/docs.yml
  lint: .tasks/lint.yml
  test: .tasks/tests.yml

tasks:
  uv-sync: uv sync --all-groups --all-extras
  pre-commit:
    desc: Run tests, auto-fix lint/format, and lint docs (CI uses lint:check)
    cmds:
      - task: test:all
      - task: lint:fix
      - task: docs:lint