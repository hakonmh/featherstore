# This workflow will install Python dependencies, run tests and lint on Python 3.11
# with the minimum versions of the dependencies on Ubuntu.

name: Ubuntu Tests

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install pytest flake8
        python -m pip install pyarrow==14.0.0
        python -m pip install pandas==2.2.0
        python -m pip install 'polars[timezone]==1.21.0'
    - name: Test with pytest
      run: |
        pytest
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --select=E9,F63,F7,F82 --show-source --statistics --count
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --exit-zero --max-complexity=10 --ignore=F405,F403 --max-line-length=100 --statistics --count
